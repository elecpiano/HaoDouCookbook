<UserControl
    x:Class="HaoDouCookBook.Controls.MealTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">
    
    <Grid x:Name="root"> 
        <Image Source="{Binding ImageSource}" Stretch="UniformToFill" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="15, 0, 0, 15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding MealName}" Style="{StaticResource MealText}"/>
                <TextBlock Text=" · " Style="{StaticResource MealText}"/>
                <TextBlock Text="{Binding Date}" TextTrimming="WordEllipsis" Style="{StaticResource MealText}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0, 3, 0, 0">
                <TextBlock Text="{Binding Number}" Style="{StaticResource MealText}"/>
                <TextBlock Text="作品" Style="{StaticResource MealText}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
