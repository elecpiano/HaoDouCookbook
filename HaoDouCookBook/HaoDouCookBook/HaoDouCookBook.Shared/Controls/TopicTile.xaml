<UserControl
    x:Class="HaoDouCookBook.Controls.TopicTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="134"
    d:DesignWidth="370">

    <Grid x:Name="root">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="62*"/>
            <ColumnDefinition Width="123*"/>
        </Grid.ColumnDefinitions>
        <Rectangle Width="122" Height="90" VerticalAlignment="Center" Grid.Column="0">
            <Rectangle.Fill>
                <ImageBrush ImageSource="{Binding TopicPreviewImageSource}"/>
            </Rectangle.Fill>
        </Rectangle>
        <Grid Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title}" Grid.Row="0" Style="{StaticResource TopicTileStyle}"/>
            <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" Margin="0,3,0,0">
                <TextBlock Foreground="Gray" Text="{Binding Author}" Style="{StaticResource TopicContentStyle}"/>
                <TextBlock Foreground="Gray" Text="{Binding CreateTimeDescription}" Margin="15,0,0,0"  Style="{StaticResource TopicContentStyle}"/>
            </StackPanel>
            <TextBlock Text="{Binding PreviewContent}" Grid.Row="2" Style="{StaticResource TopicContentStyle}" VerticalAlignment="Center" Margin="0,3,0,0"/>
        </Grid>
    </Grid>
</UserControl>
