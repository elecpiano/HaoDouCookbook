<Page
    x:Class="HaoDouCookBook.Pages.ArticleViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:HaoDouCookBook.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
    </Page.Resources>

    <Page.BottomAppBar>
        <CommandBar Style="{StaticResource bottomAppbarStyle}">
            <AppBarButton x:Name="favorite" Icon="Favorite" Label="收藏"/>
            <AppBarButton x:Name="comment" Icon="Comment" Label="评论"/>
            <AppBarButton x:Name="share" Icon="Forward" Label="分享"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="root">
        <WebView x:Name="webview" Margin="0, -105, 0, -235"/>
        <Border Background="{StaticResource TopAppbarColor}" Height="50" VerticalAlignment="Top">
            <controls:Banner x:Name="banner" VerticalAlignment="Top" Margin="5, 5, 0, 0" Title="好豆菜谱"/>
        </Border>
    </Grid>
</Page>
