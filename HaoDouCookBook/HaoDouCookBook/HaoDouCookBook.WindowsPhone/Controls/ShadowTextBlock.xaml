<UserControl
    x:Class="HaoDouCookBook.Controls.ShadowTextBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid Background="Transparent" Name="root">
        <TextBlock Text="{Binding Text}" Foreground="Black" TextTrimming="{Binding TextTrimming}" TextWrapping="{Binding TextWrapping}" MaxLines="{Binding MaxLines}">
            <TextBlock.RenderTransform>
                <TranslateTransform X="0" Y="2"/>
            </TextBlock.RenderTransform>
        </TextBlock>
        <TextBlock Text="{Binding Text}" TextTrimming="{Binding TextTrimming}" TextWrapping="{Binding TextWrapping}" MaxLines="{Binding MaxLines}" Foreground="White"/>
    </Grid>
</UserControl>
