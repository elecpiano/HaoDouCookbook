<UserControl
    x:Class="HaoDouCookBook.Controls.DiscoveryPageContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:HaoDouCookBook.Controls"
    mc:Ignorable="d"
    d:DesignHeight="3000"
    d:DesignWidth="400">

    <ScrollViewer>
        <StackPanel Orientation="Vertical">
            <controls:CategoryTitle Title="新手课堂·鸡蛋卷饼（视频）"/>
            <Border Height="300"/>

            <controls:CategoryTitle Title="煲一锅幸福爆棚的汤"/>
            <Border Height="300"/>

            <controls:CategoryTitle Title="随手晒晒"/>
            <Border Height="300"/>

            <controls:CategoryTitle Title="Ta的作品很被赞"/>
            <ItemsControl x:Name="userMasterList" Margin="2, 12, 0, 30">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <controls:UserPhoto ImageSource="{Binding UserPhoto}" Width="56" Height="56" Margin="0,0,2.5,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
