<UserControl
    x:Class="HaoDouCookBook.Controls.DishTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HaoDouCookBook.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Name="controlRoot"
    d:DesignHeight="300"
    d:DesignWidth="170">

    <StackPanel Orientation="Vertical" x:Name="root">
        <Rectangle Width="{Binding Width, ElementName=controlRoot}" Height="120" Name="imageRect" >
            <Rectangle.Fill>
                <ImageBrush ImageSource="{Binding ImageSource}"  AlignmentY="Center" Stretch="UniformToFill"/>
            </Rectangle.Fill>
        </Rectangle>
        <StackPanel Orientation="Horizontal" Margin="0, 10, 0, 0">
            <TextBlock Text="by" Style="{StaticResource DishTileTitleStyle}"/>
            <TextBlock Text="{Binding Author}" Foreground="Black" Style="{StaticResource DishTileTitleStyle}" HorizontalAlignment="Left"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0, 10, 0, 0" HorizontalAlignment="Left">
            <TextBlock Text="{Binding SupportNumber}" Style="{StaticResource DishTileSupportLineStyle}"/>
            <TextBlock Text="个赞" Margin="1, 0, 0, 0" Style="{StaticResource DishTileSupportLineStyle}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
